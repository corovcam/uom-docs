## Variables

### ~~ToolGroup~~

```ts
const ToolGroup: ToolGroupComponent;
```

Defined in: [components/assistant-ui/tool-group.tsx:218](https://github.com/corovcam/Universal-Object-Mapping/blob/main/frontend/uom-translator-ui/components/assistant-ui/tool-group.tsx#L218)

#### Deprecated

This wrapper targets the legacy `components.ToolGroup` prop
on `<MessagePrimitive.Parts>`. Use `<MessagePrimitive.GroupedParts>` with
a `groupBy` returning `"group-tool"` and compose `ToolGroupRoot` /
`ToolGroupTrigger` / `ToolGroupContent` directly. See `thread.tsx`.
