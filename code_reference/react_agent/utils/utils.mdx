---
title: utils
description: '[<span className="flex items-center gap-1"><Icon icon="external-link"/>View
  Source</span>](https://github.com/corovcam/Universal-Object-Mapping/tree/main/services/orchestrator/src/react_agent/utils/utils.py)'
---

Core utility & helper functions.

#### react\_agent.utils.utils.get\_context\_dir

```python
def get_context_dir() -> str
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L37)

Retrieve the absolute path to the local context directory.

**Returns**:

- `str` - Absolute folder path.

#### react\_agent.utils.utils.get\_config\_dir

```python
def get_config_dir() -> str
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L46)

Retrieve the absolute path to the local configuration directory.

**Returns**:

- `str` - Absolute folder path.

#### react\_agent.utils.utils.extract\_mssql\_connection\_info

```python
def extract_mssql_connection_info(
    connection_string: str
) -> dict[Literal["host", "port", "database", "user", "password"], str | int]
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L55)

Extract host, port, database, user, and password from a MSSQL connection string.

Parses standard format connection strings (e.g., 'Server=host,1333;Database=db;User Id=sa;Password=pw')
using regex to extract the individual components needed to configure the MCP Database Toolbox.

**Arguments**:

- `connection_string` _str_ - The raw MSSQL connection string.
  

**Returns**:

  dict[Literal["host", "port", "database", "user", "password"], str | int]: A dictionary of the extracted values.
  

**Raises**:

- `ValueError` - If the connection string format is invalid or cannot be parsed.

#### react\_agent.utils.utils.translate\_localhost\_to\_host\_gateway

```python
def translate_localhost_to_host_gateway(uri: str) -> str
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L91)

Translate localhost in a URI to the Docker host gateway for sandbox compatibility.

When running tools inside Daytona sandboxes (which are Docker containers), 'localhost'
refers to the container itself. This function replaces 'localhost' or '127.0.0.1' with
the host gateway IP (usually 'host.docker.internal' or an env-provided IP) so the sandbox
can reach services running on the host machine.

**Arguments**:

- `uri` _str_ - The original connection URI or string.
  

**Returns**:

- `str` - The translated URI.

#### react\_agent.utils.utils.get\_snippet\_content

```python
async def get_snippet_content(framework: FrameworkEnum,
                              is_schema: bool = False) -> str
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L111)

Read a snippet file's content based on framework and type.

#### react\_agent.utils.utils.get\_framework\_config\_content

```python
async def get_framework_config_content(framework: FrameworkEnum) -> str
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L133)

Read a framework config file's content based on framework.

#### react\_agent.utils.utils.get\_model\_creator\_map

```python
def get_model_creator_map() -> dict[AvailableModel, str]
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L150)

Return a mapping of available models to their LangChain creator class names.

#### react\_agent.utils.utils.get\_normalized\_framework\_name

```python
def get_normalized_framework_name(framework: FrameworkType) -> str
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L170)

Normalize framework names into a standardized string format for LLM prompting.

#### react\_agent.utils.utils.get\_message\_text

```python
def get_message_text(msg: BaseMessage) -> str
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L177)

Get the text content of a message.

#### react\_agent.utils.utils.load\_chat\_model

```python
async def load_chat_model(
        fully_specified_name: str,
        config: dict[str, Any] | None = None) -> BaseChatModel
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L189)

Instantiate and configure a BaseChatModel given a fully specified model string.

This complex factory function handles initialization for various LLM providers (OpenAI,
Ollama, EINFRA, etc.). It applies standard configurations like API endpoints, keys, and
timeouts. Importantly, it dynamically fetches the model's capabilities (context size,
tool calling support, structured output support) from the provider or an AI Gateway proxy
and caches this `ModelProfile`. This prevents LangChain from crashing when it doesn't
statically know if a remote model supports tools.

**Arguments**:

- `fully_specified_name` _str_ - String in the format 'provider/model'.
- `config` _dict | None_ - Optional configuration passed from the context to initialize remote parameters like API keys.
  

**Returns**:

- `BaseChatModel` - An initialized, ready-to-use LangChain chat model instance.

#### react\_agent.utils.utils.get\_model

```python
async def get_model(config: RunnableConfig,
                    runtime: Runtime[Context],
                    model_name_override: str | None = None,
                    temperature: float | None = None,
                    reasoning: bool | None = None,
                    extra_body: dict[str, Any] | None = None,
                    **chat_model_kwargs) -> BaseChatModel
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L599)

Factory to initialize the model using configuration or context.

#### react\_agent.utils.utils.get\_database\_mapping\_json

```python
async def get_database_mapping_json(
    target_framework: FrameworkEnum
) -> dict[Literal["databases", "mapping"], dict[str, Any]] | None
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L632)

Load the database mapping JSON for the given target framework.

#### react\_agent.utils.utils.create\_example\_for\_prompt

```python
async def create_example_for_prompt(framework: FrameworkEnum,
                                    return_schema: bool) -> str
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L663)

Create example code snippets for prompts based on the framework.

#### react\_agent.utils.utils.override\_pydantic\_model\_schema

```python
def override_pydantic_model_schema(
        model_cls: type[BaseModel],
        overrides: dict[str, dict[str, Any]],
        model_name: str | None = None) -> type[BaseModel]
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L674)

Dynamically modify the schema of a Pydantic model's fields at runtime.

Used by the graph to tailor the `BaseTranslationOutput` schema so the LLM doesn't waste
tokens generating irrelevant fields (like query validations when only doing a schema translation).

**Arguments**:

- `model_cls` _type[BaseModel]_ - The base Pydantic model class.
- `overrides` _dict[str, dict[str, Any]]_ - A dictionary of field names mapped to their new schema annotations and metadata.
- `model_name` _str | None_ - Optional name for the new dynamically generated model class.
  

**Returns**:

- `type[BaseModel]` - A new Pydantic model class with the applied schema overrides.

#### react\_agent.utils.utils.process\_streaming\_chunks

```python
def process_streaming_chunks(chunk: Any,
                             writer: Callable[[Any], None],
                             log_buffer: list | None = None)
```

[<span className='flex items-center gap-1'><Icon icon='external-link' />View Source</span>](https://github.com/corovcam/Universal-Object-Mapping/blob/main/services/orchestrator/src/react_agent/utils/utils.py#L716)

Process and dispatch streaming chunks from nested tools or graphs.

Writes the chunk to a designated `writer` callback (usually `runtime.stream_writer` for
custom LangGraph UI events) and optionally buffers it for logging or later aggregation.

**Arguments**:

- `chunk` _Any_ - The streamed data chunk (e.g., stdout line).
- `writer` _Callable[[Any], None]_ - The callback function to route the chunk.
- `log_buffer` _list | None_ - Optional list to append the chunk to for later inspection.

